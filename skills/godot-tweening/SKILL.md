---
name: godot-tweening
description: "C# adaptation of godot-tweening. Output Godot .NET C# only."
---

# godot-tweening — C# Skill

Use `.cs` and `csharp` only. Convert `create_tween`, signal awaits, callbacks, and property tweens to Godot C# `CreateTween()`, C# callbacks, and PascalCase property names. Do not emit new `.gd` implementation snippets.
