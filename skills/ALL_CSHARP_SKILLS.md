# Converted C# Skill Catalog

Every top-level skill in this fork has a C#-first `SKILL.md` under `skills/<name>/SKILL.md`.

Use `godot-master` for routing and `godot-csharp-mastery` for language conversion rules.
