---
name: godot-export-builds
description: "C# adaptation of godot-export-builds. Output Godot .NET C# only for Godot scripts."
---

# godot-export-builds — C# Skill

Use `.cs` and `csharp` for Godot script examples. Shell/YAML commands may remain shell/YAML when describing CI. Convert GDScript snippets to Godot C# and avoid new `.gd` implementation snippets.
