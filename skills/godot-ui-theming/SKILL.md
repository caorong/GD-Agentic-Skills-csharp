---
name: godot-ui-theming
description: "C# adaptation of godot-ui-theming. Output Godot .NET C# only."
---

# godot-ui-theming — C# Skill

Use `.cs` and `csharp` only for theme setup and runtime UI code. Theme resources may remain `.tres`; scripts that manipulate themes should use Godot C# APIs.
