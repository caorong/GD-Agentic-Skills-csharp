---
name: godot-builder
description: "C# adaptation of godot-builder. Output Godot .NET C# only where Godot code is required. Python automation helpers may remain Python."
---

# godot-builder — C# Skill

Use `.cs` and `csharp` for Godot runtime/editor examples. Python build helpers may stay Python when they are external automation scripts. Convert GDScript examples to Godot C# partial classes, lifecycle overrides, `[Export]`, `[Signal]`, typed `GetNode<T>()`, C# events, and PascalCase Godot APIs.
