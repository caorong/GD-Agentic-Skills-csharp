---
name: godot-ui-containers
description: "C# adaptation of godot-ui-containers. Output Godot .NET C# only."
---

# godot-ui-containers — C# Skill

Use `.cs` and `csharp` only. Convert GDScript UI examples to Godot C# partial classes, lifecycle overrides, `[Export]`, `[Signal]`, typed `GetNode<T>()`, C# events, and PascalCase Control APIs.
