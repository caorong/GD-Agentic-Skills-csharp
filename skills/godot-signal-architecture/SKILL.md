---
name: godot-signal-architecture
description: "C# adaptation of godot-signal-architecture. Output Godot .NET C# only."
---

# godot-signal-architecture — C# Skill

Use C# events and Godot C# signal delegates. Convert `signal`, `connect`, and `emit_signal` to `[Signal]`, event subscription, and `EmitSignal(SignalName.X)`. Do not emit new `.gd` implementation snippets.
